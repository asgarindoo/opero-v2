import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account — OPERO",
  description: "Start your free OPERO account. No credit card required. Manage your entire business workflow from day one.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
