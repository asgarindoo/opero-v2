export const metadata = {
  title: "Flows | OPERA",
  description: "Manage operational workflows and lifecycle structures",
};

export default function FlowsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
